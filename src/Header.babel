import React, { Component } from 'react';

export class Header extends Component {
	render() {
		return (
			<div className="header">
				<h1 className="header-title">{this.props.data['title']}</h1>
				<h2 className="header-subtitle">{this.props.data['subtitle']}</h2>
			</div>
		);
	}
}